// Importing the environment setting - This is important to determine what CSS file will use in dev mode (styles.css) or production mode (styles.min.css)
import '../../config/mode.js';

// Write here all general interactivity of your application, for example: nav links a chat button, etc. You have to import it into all pages of your app as need.
